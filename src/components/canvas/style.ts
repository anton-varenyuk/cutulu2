import { css } from '@emotion/css';
import { FONTSIZE, spacing } from '../style';

export const exampleWrapper = css`
	display: flex;
	gap: ${spacing(4)};
`;

export const canv = css`
	background: #36373b;
`;

export const implementationPreview = css`
	font-size: ${FONTSIZE.regular};
`;
